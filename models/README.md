# models/

Wrappers, configs, prompts, and provider routing for AI models used by the
sync agent. No model weights, API keys, or secrets are stored here — reference
environment variables / a secrets manager instead.
